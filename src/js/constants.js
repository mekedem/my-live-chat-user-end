/**
 * this fields will be added from the backend.
 * they are here only for reference on what to expect from the server.
 * use them without import as if they exist.

const PROJECT_ID = '5f6276e57f237845109bbbff';

const SETTINGS = {
    active: true,
    waitTime: 0,
    soundNotification: false,
    browserTabNotification: false,
    showAgentTyping: false,
    showVisitorTyping: false,
    sneakPreview: false,
    hideWidgetWhenOffline: false,
    hideWidgetOnMobile: false,
    fileUploadAllowed: false,
    chatRatingAllowed: false,
    emojiInChatAllowed: false,
};
**/

const CHAT_ASSET_SERVER_URL = 'http://localhost:5000';
export default CHAT_ASSET_SERVER_URL;
